{
  "name": "callGoogleVisionOcr",
  "description": "Extract text using Google Vision OCR from a base64-encoded image.",
  "parameters": {
    "type": "object",
    "properties": {
      "image_base64": {
        "type": "string"
      },
      "language_hint": {
        "type": "string"
      }
    },
    "required": [
      "image_base64"
    ]
  }
}